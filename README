<공통>
1. git clone <repo 주소>
2. cd onDongne
3. npm install
4. npm run dev:all

<백엔드>
#서버만 켜기: 루트에서 npm run dev:backend
1. cd Backend
2. npm install
3. npm run dev
4. .env파일은 git에 올라가지않음으로 직접 추가해야함.
5. CORS 이슈 발생 시: Backend/.env의 CLIENT_ORIGIN이 프론트 실제 주소(예: http://localhost:8081)와 일치하는지 확인
→ 서버 재시작


<프론트엔드>
#프론트엔드만 켜기: 루트에서 npm run dev:frontend
1. cd Frontend
2. npm install
3. npm run web / npm run start
#\onDongne\Backend\docs\swagger.yaml 엔 API관련 명세들이 적혀있습니다

--------------------------------설치가 잘 안돼요----------------------------------
1. git 설치
2. node.js 설치
3. 클론한 레포 터미널에서 npm install 했는데 "npm : 이 시스템에서 스크립트를 실행할 수 없으므로...자세한 내용은 about_Execution_Policies.."
    에러 발생시 아래 문구 터미널에 입력
     Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
4. 다시 npm install
5. <공통>의 4번(npm run dev:all) 실행
6. 웹페이지에 뭐라도 뜨면 성공

#package.json에 설치된 의존성들이 나와있습니다
